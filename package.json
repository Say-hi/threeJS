{
  "name": "gulp-test",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browser": "echo \"browserSync start\" && browser-sync start --server --files '**/*.html,**/*.js,**/*.css'",
    "http": "echo \"Http server start\" && hs -o -p 9999"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1"
  },
  "dependencies": {
    "jquery": "^3.2.1"
  }
}
